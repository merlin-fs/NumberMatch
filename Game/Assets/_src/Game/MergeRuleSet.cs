using System.Collections.Generic;
using System.Linq;
using Game.Core.Data;

namespace Game.Core
{
    /*
    public class MergeRuleSet
    {
        private HashSet<IMergeRule> _rules = new ();

        public bool CanMerge(FieldData field, int y1, int x1, int y2, int x2)
        {
            return _rules.Any(rule => rule.CanMerge(field, y1, x1, y2, x2));
        }

        public void AddRule(IMergeRule rule)
        {
            if (rule == null) return;
            _rules.Add(rule);
        }
    }
    */
}